const SearchResult = ({data}) => {
  const val = data;
  console.log(val);
  return (
    <>
      {/* {val.map((v) =>
        Object.values(v).map((v1) => {
          return <p>{v1}</p>;
        }),
      )} */}
    </>
  );
};

export default SearchResult;
